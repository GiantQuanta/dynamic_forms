<div class="preview dynamic_forms">
  <p id="notice"><%= notice %></p>

  <h1>
    <%= @form.title %>
  </h1>

  <%= simple_format @form.description %>

  <% @form.items.each do |item| %>
    <%= render item %>
  <% end %>

</div>

<%= link_to 'Edit', edit_form_path(@form) %> |
<%= link_to 'Back', forms_path %>

